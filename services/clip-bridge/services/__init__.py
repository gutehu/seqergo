# CLIP zero-shot for SeqErgo
# First run downloads openai/clip-vit-base-patch32 (~600 MB).
# Phone and PC must be on the same Wi-Fi. Use the PC LAN IP in the app.
# python3 -m venv .venv && .venv/bin/pip install -r requirements.txt && ./run.sh
